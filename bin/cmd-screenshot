#!/bin/bash

pkill slurp \
  || grim -g "$(slurp -w 1 -d)" -t ppm - \
  |  satty --filename - \
           --early-exit \
           --actions-on-enter save-to-clipboard \
           --save-after-copy \
           --copy-command 'wl-copy'
